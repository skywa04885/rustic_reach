import { Circle } from "@react-three/drei";
import React from "react";

export const MyEditorPreviewOrientEndEffectorTool = (): React.ReactElement => {
  return (
    <group>
      
    </group>
  );
}